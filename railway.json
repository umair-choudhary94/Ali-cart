{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "commands": {
      "setup": "python -m venv env && . env/bin/activate && pip install -r requirements.txt"
    }
  },
  "deploy": {
    "startCommand": ". env/bin/activate && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn mysite.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
